apiVersion: v1
kind: ServiceAccount
metadata:
  # Name must stay exactly "draw" — the draw-objects IRSA trust matches system:serviceaccount:<ns>:draw
  name: draw
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::048751351548:role/app/draw-objects
